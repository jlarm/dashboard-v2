import FrontEndComplianceForm from './FrontEndComplianceForm'
import GlobalSettings from './GlobalSettings'

const Settings = {
    FrontEndComplianceForm: Object.assign(FrontEndComplianceForm, FrontEndComplianceForm),
    GlobalSettings: Object.assign(GlobalSettings, GlobalSettings),
}

export default Settings