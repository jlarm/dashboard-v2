import osha from './osha'
import bodyShop from './body-shop'
import finance from './finance'
import individual from './individual'
import dealJackets from './deal-jackets'
import dealJacketReports from './deal-jacket-reports'

const audit = {
    osha: Object.assign(osha, osha),
    bodyShop: Object.assign(bodyShop, bodyShop),
    finance: Object.assign(finance, finance),
    individual: Object.assign(individual, individual),
    dealJackets: Object.assign(dealJackets, dealJackets),
    dealJacketReports: Object.assign(dealJacketReports, dealJacketReports),
}

export default audit